{
  "name": "pac-man-my-build",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
  "serve": "http-server -p 3000 -a localhost ./",
  "start": "concurrently \"npm run serve\" \"npm run electron\"",
  "electron": "wait-on http://localhost:3000 && electron ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "electron": "^36.3.1",
    "http-server": "^14.1.1",
    "wait-on": "^8.0.3"
  }
}
